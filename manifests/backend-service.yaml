apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: amex
  labels:
    app: backend
spec:
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  selector:
    app: backend
  type: ClusterIP
