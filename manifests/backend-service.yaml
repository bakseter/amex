apiVersion: v1
kind: Service
metadata:
  name: amex-backend
  namespace: amex
spec:
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  selector:
    app.kubernetes.io/name: amex-backend
  type: ClusterIP
