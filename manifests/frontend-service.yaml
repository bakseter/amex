apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: amex
  labels:
    app: frontend
spec:
  ports:
    - name: http
      port: 3000
      targetPort: 3000
  selector:
    app: frontend
  type: ClusterIP
