apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: amex-database
  namespace: amex
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: amex-database
  podMetricsEndpoints:
    - port: metrics
