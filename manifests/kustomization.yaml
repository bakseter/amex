apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - includeSelectors: true
    pairs:
      app.kubernetes.io/instance: amex

resources:
  - ./backend
  - ./database
  - ./frontend
  - httproute.yaml
